# RUthirsty 喝水打卡应用 - 快速开始

最快5分钟构建你的喝水打卡APK！

## 超快速构建（适合有Android环境的用户）

```bash
# 1. 克隆项目
git clone <your-repo>
cd RUthirsty-cordova

# 2. 安装依赖
npm install

# 3. 添加Android平台
cordova platform add android

# 4. 构建调试APK
./cordova-apk-builder/scripts/build_debug.sh

# ✅ 完成！APK位置：
# platforms/android/app/build/outputs/apk/debug/app-debug.apk
```

## 环境要求检查

运行以下命令检查环境是否就绪：

```bash
cordova requirements android
```

如果显示全部安装完成，你就可以开始构建了！

如果有缺失项，请参考 [APK构建指南.md](./APK构建指南.md) 完成环境配置。

## 三步构建流程

### 第1步：构建APK

**调试版（用于测试）：**
```bash
./cordova-apk-builder/scripts/build_debug.sh
```

**发布版（用于分发）：**
```bash
# 首次需要生成签名密钥
./cordova-apk-builder/scripts/generate_keystore.sh

# 然后构建
./cordova-apk-builder/scripts/build_release.sh
```

### 第2步：安装到设备

```bash
# 连接Android设备或启动模拟器，然后：
./cordova-apk-builder/scripts/install_apk.sh
```

### 第3步：测试

打开设备上的"RUthirsty"应用，测试：
- ✅ 点击喝水打卡按钮
- ✅ 查看今日喝水次数
- ✅ 浏览喝水记录列表
- ✅ 关闭并重新打开应用，数据应该保留

## 常见问题快速解决

### 问题1：找不到cordova命令
```bash
npm install -g cordova
```

### 问题2：ANDROID_HOME未设置
```bash
# macOS/Linux
export ANDROID_HOME=~/Library/Android/sdk  # macOS
export ANDROID_HOME=~/Android/Sdk          # Linux

# Windows (在系统环境变量中设置)
# ANDROID_HOME = C:\Users\<用户名>\AppData\Local\Android\Sdk
```

### 问题3：构建失败
```bash
# 清理并重新构建
cordova clean
cordova platform rm android
cordova platform add android
cordova build android
```

## 发布准备清单

准备发布到应用商店？确保完成以下步骤：

- [ ] 更新 `config.xml` 中的版本号
- [ ] 生成并妥善保管签名密钥
- [ ] 构建已签名的发布版APK
- [ ] 在多个设备上测试
- [ ] 准备应用图标和截图
- [ ] 撰写应用描述
- [ ] 准备隐私政策

## 需要帮助？

- 📖 详细指南：[APK构建指南.md](./APK构建指南.md)
- 🛠️ Skill文档：[cordova-apk-builder/SKILL.md](./cordova-apk-builder/SKILL.md)
- 🔐 签名指南：[cordova-apk-builder/references/signing_guide.md](./cordova-apk-builder/references/signing_guide.md)

---

**快速提示：** 如果你只想预览应用效果而不构建APK，在浏览器中打开 http://localhost:8080 (需先运行 `cordova serve`)
